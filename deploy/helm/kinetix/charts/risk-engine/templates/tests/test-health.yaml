apiVersion: v1
kind: Pod
metadata:
  name: {{ include "risk-engine.fullname" . }}-test-health
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: test-health
      image: busybox
      command: ["wget", "-qO-", "http://{{ include "risk-engine.fullname" . }}:{{ .Values.service.metricsPort }}/metrics"]
