apiVersion: v1
kind: Pod
metadata:
  name: {{ include "correlation-service.fullname" . }}-test-health
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: test-health
      image: busybox
      command: ["wget", "-qO-", "http://{{ include "correlation-service.fullname" . }}:{{ .Values.service.port }}/health"]
