apiVersion: v1
kind: Pod
metadata:
  name: {{ include "rates-service.fullname" . }}-test-health
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: test-health
      image: busybox
      command: ["wget", "-qO-", "http://{{ include "rates-service.fullname" . }}:{{ .Values.service.port }}/health"]
